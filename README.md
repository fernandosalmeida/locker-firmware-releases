# Firmware Release Artifacts

This repository stores public firmware release artifacts only.

Documentation is centralized in:

```text
git@github.com:locker-contracts/contracts.git
```

See `contracts/repositories/locker-firmware-releases/` for the migrated README and `contracts/ota/`
for OTA contracts. Do not add independent documentation here.
